// Stores first name in a variable
var firstName = prompt("What is your First Name ");

// // Stores last name in a variable
var lastName = prompt("What is your last name ");

// Stores age in a variable
var age = prompt("What is your age ");

// combines first name and last name into one variable
var fullName = firstName + " " + lastName;

    // print our first name + last name with a prompt
console.log("Your full name is " + fullName);

// print out message along with age plus years old
console.log("Your are " + age + " years old");